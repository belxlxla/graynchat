name: Deploy to AWS with GHCR

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. 🆕 Node.js 환경 설정 (GitHub 서버에 노드 설치)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm' # npm 캐시를 사용하여 속도 업!

      # 2. 🆕 의존성 패키지 설치
      - name: Install dependencies
        run: npm install

      # 3. 🆕 프로젝트 빌드 (여기서 최신 dist 폴더가 생성됩니다)
      - name: Build project
        run: npm run build

      # 4. 이미지 주소 소문자 변환
      - name: Set up Image Name to Lowercase
        run: echo "IMAGE_ID=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      # 5. GHCR 로그인
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 6. 도커 이미지 빌드 및 푸시
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ env.IMAGE_ID }}:latest
            ghcr.io/${{ env.IMAGE_ID }}:${{ github.sha }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            # 1. 이미지 이름을 소문자로 정의 (빌드 시 이름과 동일해야 함)
            IMAGE_ID=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')
            
            # 2. GHCR 로그인
            echo "${{ secrets.GITHUB_TOKEN }}" | sudo docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # 3. 프로젝트 경로 이동
            cd ~/graynserver

            # 4. 최신 이미지 가져오기
            sudo docker pull ghcr.io/$IMAGE_ID:latest

            # 5. docker-compose 재시작 (새 이미지가 반영되도록 함)
            # 주의: docker-compose.yml 파일 내의 image 이름도 ghcr.io/계정/레포:latest 형식이어야 합니다.
            sudo docker-compose up -d --remove-orphans

            # 6. 불필요한 이미지 정리
            sudo docker image prune -f